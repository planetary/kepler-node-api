module.exports =
    'concurrency': 4  # now many screen captures can be done in parallel
    'phantom': 'phantomjs'  # path to phantom js binary (defaults to PATH)
    'timeout': 60000  # max runtime in milliseconds
