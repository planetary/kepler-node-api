# Kepler

Screenshot service

## Dependencies

* NodeJS
* MongoDB
* PhantomJS
* AWS bucket

## Installation
* `sudo npm install -g gulp migrate mocha`
* `npm install`
* `gulp build`

## Serve Locally
* `gulp`
